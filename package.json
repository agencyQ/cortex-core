{
  "name": "@agencyq-007/cortex-core",
  "version": "1.0.0",
  "description": "AgencyQ Claude Code setup CLI — bootstrap any repo with consistent rules, hooks, skills, and MCPs",
  "type": "module",
  "bin": {
    "cortex-core": "./dist/cli.js"
  },
  "files": [
    "dist/",
    "templates/"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inquirer/prompts": "^5.0.0",
    "chalk": "^5.0.0",
    "commander": "^12.0.0",
    "fs-extra": "^11.0.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.0",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
